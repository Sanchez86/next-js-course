import { Metadata } from "next";

export const metadata: Metadata ={
    title: 'Контакты'
}

const Contacts = () => {
    return (
        <div>
            <h2>+30633389475</h2>
        </div>
    );
}

export default Contacts;